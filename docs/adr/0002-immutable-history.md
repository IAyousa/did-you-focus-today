# 历史记录只读，不可修改删除

已写入的历史（完成的番茄、中断记录）不提供任何修改或删除入口。这是给"记录型督促"上锁：能改历史，就等于能给自己洗数据，记录型督促随之失效。代价是极小概率的误记（如浏览器崩溃被补记为中断）无法纠正——诚实性的价值高于偶尔一条冤假错案。这是有意设计而非功能缺失，后续扩展时不要顺手加"编辑记录"功能。

## Consequences

- 导入 JSON 备份会整体替换本地数据，这是绕过只读性的唯一通道，因此导入必须显式确认
